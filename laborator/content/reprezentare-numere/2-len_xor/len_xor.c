#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int my_strlen(const char *str)
{
	/* TODO */
}

void equality_check(const char *str)
{
	/* TODO */
	// str[i] == str[i + pow(2, i)]
	// memcmp(str + ..., str + ..., sizeof(*str));
}

int main(void)
{
	/* TODO: Test functions */

	return 0;
}

